const Contact = () => {
	// const [number, setNumber] = useState({ a: 1, b: 2, c: 3, name: "john" });
	// console.log(number["a"]);
	return (
		<div>
			<div>Contact Page</div>
			{/* <p>Munculkan angka {number.a}</p>
			<button
				className="bg-gray-200 py-2 px-6 rounded-sm"
				onClick={() => setNumber({ ...number, a: number.a + 1 })}
			>
				Plus
			</button> */}
		</div>
	);
};

export default Contact;
